// this file is to host a JobForm, and deal with the sending and retrieving of information from the backend
import { JobForm } from "./JobForm"
const endpoint = 'localhost';

export function JobSearch() {
    return (
        <JobForm />
    )
}

